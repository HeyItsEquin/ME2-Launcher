using System;

namespace ME2Launcher
{
    public static class ConfigGenerator
    {
        public static void GenerateConfigFile(Profile profile, string path)
        {
            if (profile == null)
                return;
        }
    }
}
